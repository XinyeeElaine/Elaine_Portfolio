/* Template — committed to git. Copy to config.js and fill in real values. */
/* config.js is gitignored so your real webhook URL stays out of the repo. */
window.CHAT_WEBHOOK_URL = 'https://YOUR-N8N-HOST/webhook/elaine-chat';
